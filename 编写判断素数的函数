n=int(input());
def isnum(n):
    for i in range (2,n):
        if(n%i==0):return 0;
    return 1;
if(n!=1):
    if(isnum(n)):print('{}是素数'.format(n));
    else :print('{}不是素数'.format(n));
else:print('{}不是素数'.format(n));
